
<% if @users.nil? or @users.empty? %>
  No results
<% else %>
  <ul class="list">
    <%= render :partial => 'users/users', :locals => { :users => @users } %>
  </ul>
  <%= will_paginate @users%>
<% end %>
