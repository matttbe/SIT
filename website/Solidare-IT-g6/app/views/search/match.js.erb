$('.services').append('<%= escape_javascript(render :partial => 'services', :locals => { :services => @services } )%>');
$('a.load-more-services').attr('href', '<%=services_path(:page => @services.next_page)%>');
if ($('a.load-more-services').length==0) {
	$('.services').append('<%= link_to 'Load More Services', services_path(:page => @services.next_page), :class => 'load-more-services', :remote => true %>');
	$('a.load-more-services').on('inview', asyncSearch)
}
<% unless @services.next_page %>
  $('a.load-more-services').remove();
<% end %>
