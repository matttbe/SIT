$(".services ul").append('<%= escape_javascript(render :partial => 'services', :locals => { :services => @services } )%>');
serviceList.update()
$('#loader').remove();
$('a.load-more-services').attr('href', '<%=services_path(:page => @services.next_page)%>');
<%= will_paginate @services %>