<%= form_for(@service) do |f| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="field">
  What do you want to create ?<br>
<label class="radio">
  <input type="radio" name="service.demand" id="service_demand" value="demand" checked>
  Demand
</label>
<label class="radio">
  <input type="radio" name="service.demand" id="service_offer" value="offer">
 Offre
</label>
   </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :date_start %><br>
    <%= f.datetime_select :date_start %>
  </div>
  <div class="field">
    <%= f.label :date_end %><br>
    <%= f.datetime_select :date_end %>
  </div>
  <div class="actions">
    <% if @service.creator_id.to_f <1%>
      <%= f.submit "Create service",:class =>"btn btn-primary"  %>
    <% else %>
      <%= f.submit "Edit service",:class =>"btn btn-primary"  %>
    <%end%>
  </div>
<% end %>
