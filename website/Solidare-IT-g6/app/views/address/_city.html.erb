
<% if defined?(address) && !address.nil? %>
  <% @address = address %>
<% end %>

<strong>Location: </strong>
  <% if @address.postal_code != 0 %>
    <%= @address.postal_code %>, 
  <% end %>
  <%= @address.city %> — 
  <%= @address.country%>
