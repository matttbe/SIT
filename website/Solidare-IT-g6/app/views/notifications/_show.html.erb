
<%if @notifications.size > 0 %>
	<% @notifications.each do |notif| %>	
		<% @users.each do |user| %>
			<%if user.id == notif.service.creator_id %>
				<% @service_creator = user %>
			<% elsif user.id == notif.creator_id %>
				<% @creator = user %>				
				<% @creator_fullname = " "+@creator.firstname+" "+@creator.name+" " %>
			<%end%>
		<%end%>
		
		<% @service_string = "The service \"" + notif.service.title + "\""%>
		
		<% case notif.notification_type %>
		<%when 'EDIT'%>
			<%= @service_string %> of <%= @service_creator.firstname %> <%= @service_creator.name %> was eddited<br />
		<%when 'ACCEPT'%>
			<%= @service_string %> was accepted by <%= link_to @creator_fullname, show_profile_path(notif.creator_id)%> <br />
		<%when 'FOLLOW'%>
			<%= @service_string %> was followed by <%= link_to @creator_fullname, show_profile_path(notif.creator_id)%> <br />
		<%when 'UNFOLLOW'%>
			<%= @service_string %> was unfollowed by <%= link_to @creator_fullname, show_profile_path(notif.creator_id)%> <br />
		<%else%>
			NO MESSAGE FOR THIS TYPE
		<%end %>
	<%end%>
<%else%>
	Vous n'avez pas de notification
<%end%>
